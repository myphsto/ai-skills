{
  "skill_name": "linux-cloud-init",
  "evals": [
    {
      "id": 1,
      "prompt": "I'm designing or validating `cloud-init` user-data for first boot on an Ubuntu 24.04 server. Where should I start?",
      "expected_output": "Checks /etc/os-release and picks the right family commands before running anything. Reads references/autoinstall-reference.md, references/debugging.md for the detailed procedure. Guides the task with standard tools, validates configuration before any reload or restart, asks for confirmation before destructive steps, and verifies the end state (service status, config, or output) before declaring success.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "I'm debugging why a cloud-init or autoinstall run failed on a Debian 12 server. What's the safe way to do this without taking anything down?",
      "expected_output": "Checks /etc/os-release and picks the right family commands before running anything. Reads references/autoinstall-reference.md, references/debugging.md for the detailed procedure. Guides the task with standard tools, validates configuration before any reload or restart, asks for confirmation before destructive steps, and verifies the end state (service status, config, or output) before declaring success.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "I'm bootstrapping a fresh Ubuntu server from declarative YAML rather than manual provisioning on a Rocky Linux 9 server. Walk me through it step by step.",
      "expected_output": "Checks /etc/os-release and picks the right family commands before running anything. Reads references/autoinstall-reference.md, references/debugging.md for the detailed procedure. Guides the task with standard tools, validates configuration before any reload or restart, asks for confirmation before destructive steps, and verifies the end state (service status, config, or output) before declaring success.",
      "files": []
    }
  ]
}
