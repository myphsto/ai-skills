{
  "skill_name": "linux-image-hygiene",
  "evals": [
    {
      "id": 1,
      "prompt": "I need to a container host is filling its disk (`/var/lib/docker`, container storage) on an Ubuntu 24.04 server. Where should I start?",
      "expected_output": "Checks /etc/os-release and picks the right family commands before running anything. Reads references/prune-and-scheduling.md for the detailed procedure. Guides the task with standard tools, validates configuration before any reload or restart, asks for confirmation before destructive steps, and verifies the end state (service status, config, or output) before declaring success.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "I'm removing dangling images, stopped containers, unused volumes/networks, build cache on a Debian 12 server. What's the safe way to do this without taking anything down?",
      "expected_output": "Checks /etc/os-release and picks the right family commands before running anything. Reads references/prune-and-scheduling.md for the detailed procedure. Guides the task with standard tools, validates configuration before any reload or restart, asks for confirmation before destructive steps, and verifies the end state (service status, config, or output) before declaring success.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "I'm setting up automatic, scheduled cleanup with a systemd timer on a Rocky Linux 9 server. Walk me through it step by step.",
      "expected_output": "Checks /etc/os-release and picks the right family commands before running anything. Reads references/prune-and-scheduling.md for the detailed procedure. Guides the task with standard tools, validates configuration before any reload or restart, asks for confirmation before destructive steps, and verifies the end state (service status, config, or output) before declaring success.",
      "files": []
    }
  ]
}
